# 卦LISP 设定稿

## 1. 项目定位

**卦LISP** 是一门用于探索中国传统玄学与计算机语言融合的实验性语言。

它关注的不是单一的《周易》系统，而是更广义的玄学、术数与象数传统，包括但不限于：

- 易经、周易
- 阴阳、八卦、六十四卦
- 风水
- 奇门遁甲
- 其他可被形式化、符号化、计算化的传统玄学系统

卦LISP 的目标不是把玄学简单翻译成程序语法，也不是只做一个中文编程玩具。它想探索的是：

> 如何把中国传统玄学中的“象”“数”“变”“位”“时”“势”，转化为计算机可以表达、运行、观察和扩展的语言机制。

底层思想上，卦LISP 将 **阴阳** 与计算机的 **二进制** 相连接：

- 阳对应 `1`
- 阴对应 `0`
- 爻对应位
- 卦对应状态结构
- 变卦对应状态转换
- 象对应可被观察、解释、组合的运行表现

## 2. 为什么叫“卦LISP”

本项目不叫 **周易LISP**，也不叫 **易LISP**。

原因是：卦LISP 并不只服务于周易，也不只表达“易”的哲学。周易是重要源头，但它不是全部。项目真正想呈现的是一种更广义的 **玄学之相**。

这里的“相”，不是一个固定功能，也不是一个必须有用的工具。它更像一种显现：

> 本来没有什么实际用处，但它现在就是出现了。

这种“出现了的相”，正是卦LISP 的气质。它既可以被当作语言设计实验，也可以被当作文化计算、艺术编程、符号系统、玄学模拟、教学玩具，甚至只是一种带有东方神秘气息的代码形式。

“卦”这个名字保留了语言的核心象征：

- 卦连接阴阳与二进制
- 卦表达状态与变化
- 卦可以作为内部机制承载玄学系统
- 卦足够开放，可以容纳周易之外的术数系统

因此，**卦LISP** 是一个比“周易LISP”更宽的名字。

## 3. 卦式表达与内层机制

卦LISP 的表层语法不再以传统 Lisp 为核心，而是尽量直接使用卦名和玄学概念表达计算需求。

这门语言的目标用户本身就需要理解卦象、象数、术数和相关玄学机制。因此，卦象不必被隐藏在普通关键字后面。相反，卦名可以直接成为表达式的核心动词。

例如：

```gua
益 a b c
```

表示：

```ts
a + b + c
```

这里的 `益` 不是普通关键字的中文替代，而是借用益卦“增益、增加、助长”的含义表达加和。

### 内层：卦象机制

卦象既可以作为表层表达式，也可以作为语言内部机制存在。

运行时可以把变量、函数、状态、条件、分支、循环等结构映射到卦象系统中。用户可以通过以下方式观察或利用这些机制：

- 查看运行日志
- 查看表达式对应的卦象
- 查看状态变化产生的变卦
- 使用调试工具观察阴阳变化
- 在进阶阶段主动调用卦象机制实现功能

也就是说：

> 卦既是语言的表达方式，也是语言的运行骨架。

## 4. 玄学作为函数组

卦LISP 不把所有玄学体系一次性塞进语言核心。

更合理的模型是：语言核心保持小而清楚，玄学体系作为一组可扩展的函数、模块或库存在。

可以类比 TypeScript 里的库、类型系统和标准模块：

- 卦式语法负责表达
- 运行时负责求值
- 卦象机制负责状态、变化和象的生成
- 玄学模块负责提供具体术数能力

例如未来可以有：

```gua
入 周易
入 风水
入 奇门

起卦 时间 问题
排盘 奇门 时间 方位
观象 当前状态
```

这意味着：

- 周易是一组函数和解释系统
- 风水是一组空间、方位、气场相关的函数
- 奇门遁甲是一组时间、局、门、星、神、方位相关的函数
- 卦象是更底层的状态与变化机制

语言核心不需要预设所有玄学答案，而是提供一个可以承载这些系统的计算框架。

## 5. 核心思想

卦LISP 的核心思想可以暂定为：

### 阴阳即二进制

计算机底层是 `0` 与 `1`，传统玄学底层是阴与阳。卦LISP 将两者视为可以互相映照的符号基础。

### 卦象即状态

卦可以表示一个程序片段、变量、函数、对象、流程或运行瞬间的状态。

### 变卦即计算

程序执行不是静态文本，而是状态不断变化的过程。计算可以被理解为从一个卦象走向另一个卦象。

### 象不是答案，而是观察方式

卦LISP 不强行宣称某个卦象就是唯一解释。它更关注“如何生成象”“如何观察象”“如何让象参与计算”。

### 玄学系统是可插拔的

周易、风水、奇门遁甲等体系不必全部进入语言核心。它们可以作为不同模块叠加在同一个卦式运行时之上。

## 6. 阶段规划

当前阶段：**设定阶段**。

这一阶段先明确：

- 卦LISP 的名字与气质
- 表层语法与内层卦象机制的关系
- 玄学体系与语言核心的关系
- 语言应该偏实验、文化、艺术还是实用
- 第一版 MVP 应该收敛到哪些能力

下一阶段才进入 MVP：

- 最小语法
- 最小解释器或转译器
- 基础变量、函数、条件、输出
- 运行日志中的卦象映射
- 一个最小的周易/起卦示例模块

## 7. 暂定原则

- 表达直接，内层玄妙。
- 用户先能写程序，再逐渐理解卦象机制。
- 卦不是噱头，而是运行时状态模型。
- 玄学不是硬编码进语法，而是作为函数组与模块扩展。
- 周易是源头之一，但卦LISP 不被周易限制。
- 语言可以没有现实用途，但必须有自己的“相”。

## 8. 语言方向修正：去 Lisp 外壳

经过进一步讨论，卦LISP 去掉传统 Lisp 的外壳。

也就是说，语言不再优先采用：

```txt
(益 a b)
```

而是可以更直接地写成：

```gua
益 a b
```

表示：

```ts
a + b
```

如果有多个参数：

```gua
益 a b c d e f
```

表示：

```ts
a + b + c + d + e + f
```

这意味着：用户最终面对的不是 Lisp，而是以卦名、玄学概念和中文语义为核心的表达式系统。

如果用户本来就是熟悉卦象、术数、玄学系统的人，那么语言不需要刻意隐藏复杂性。相反，卦象机制可以直接成为语言的主要表达方式。

## 9. 编译目标

卦LISP 后期优先编译到 **TypeScript**。

原因：

- TypeScript 当前生态成熟
- TypeScript 对 AI 工具、编辑器、类型提示较友好
- TypeScript 可以运行在 Node.js、浏览器、服务端等环境
- TypeScript 适合作为第一阶段的中间目标语言

同时，卦LISP 也可以考虑编译到 **Python**。

原因：

- Python 生态适合科学计算、AI、数据处理、自动化
- Python 方便调用大量现成库
- Python 更适合某些玄学、命理、排盘、数据分析类模块

因此，语言设计时应尽量让核心语义能稳定映射到：

- TypeScript
- Python

TypeScript 可以作为主编译目标，Python 作为后续重要目标。

## 10. 卦名与基础表达式映射草案

卦名选择原则：

> 能用卦本身的含义反映计算用途，就优先使用该卦。

不强求一次性用满六十四卦。常用能力优先选择含义自然、容易联想的卦。如果单个卦不够用，可以扩展为双字、三字名称。

例如：

- `益` 表示增加、加和
- `损` 表示减少、减法
- `益革` 表示增量赋值
- `损革` 表示减量赋值

### 基础值

| TS | 卦名 | 含义 |
|---|---|---|
| `true` | 乾 | 阳、真、成立、开启 |
| `false` | 坤 | 阴、假、不成立、关闭 |
| `null` | 坎 | 空位、陷处、有坑但存在 |
| `undefined` | 未济 | 未完成、未定、尚未成形 |

### 算术表达式

| TS | 卦名 | 示例 |
|---|---|---|
| `+` | 益 | `益 a b c` = `a + b + c` |
| `-` | 损 | `损 a b` = `a - b` |
| `*` | 丰 | `丰 a b` = `a * b` |
| `/` | 节 | `节 a b` = `a / b` |
| `%` | 剥 | `剥 a b` = `a % b` |
| `**` | 升 | `升 a b` = `a ** b` |
| `++` | 晋 | `晋 a` = `a++` |
| `--` | 蹇 | `蹇 a` = `a--` |

### 比较表达式

| TS | 卦名 | 示例 |
|---|---|---|
| `===` | 同人 | `同人 a b` |
| `!==` | 睽 | `睽 a b` |
| `>` | 大过 | `大过 a b` |
| `<` | 小过 | `小过 a b` |
| `>=` | 大畜 | `大畜 a b` |
| `<=` | 小畜 | `小畜 a b` |

### 逻辑表达式

| TS | 卦名 | 示例 |
|---|---|---|
| `&&` | 比 | `比 a b` |
| `||` | 萃 | `萃 a b` |
| `!` | 否 | `否 a` |
| `??` | 需 | `需 a b` |

### 变量与赋值

| TS | 卦名 | 示例 |
|---|---|---|
| `const` | 乾 | `乾 名 值` |
| `let` | 坤 | `坤 名 值` |
| `=` | 革 | `革 名 值` |
| `+=` | 益革 | `益革 名 值` |
| `-=` | 损革 | `损革 名 值` |

示例：

```gua
乾 年龄 18
坤 计数 0
革 计数 10
益革 计数 1
```

### 控制流

| TS | 卦名 | 示例 |
|---|---|---|
| `if` | 观 | `观 条件 成立 不成立` |
| `switch` | 噬嗑 | `噬嗑 值 分支...` |
| `while` | 需 | `需 条件 行为` |
| `for` | 渐 | `渐 初始 条件 步进 行为` |
| `break` | 夬 | `夬` |
| `continue` | 复 | `复` |
| `return` | 归妹 | `归妹 值` |

### 函数与调用

| TS | 卦名 | 示例 |
|---|---|---|
| `function` | 鼎 | `鼎 名 参数 体` |
| `call` | 应 | `应 函数 参数...` |
| `return` | 归妹 | `归妹 值` |

`鼎` 可理解为函数：输入进入鼎中，经由内部变化，炼化为输出。

示例：

```gua
鼎 求和 a b
  归妹 益 a b

应 求和 3 5
```

### 集合与结构

| TS | 卦名 | 示例 |
|---|---|---|
| `Array` | 萃 | `萃 1 2 3` |
| `Object` | 家人 | `家人 名 "张三" 年龄 18` |
| property access | 观 | `观 用户 年龄` |
| index access | 临 | `临 列表 0` |
| spread `...` | 涣 | `涣 列表` |

### 模块

| TS | 卦名 | 示例 |
|---|---|---|
| `import` | 入 | `入 周易` |
| `export` | 出 | `出 起卦` |
| namespace/module | 家人 | `家人 周易 ...` |

## 11. 命名扩展原则

卦名不必全部用完，也不必强迫所有语义都塞进六十四卦。

当单个卦不够表达时，可以扩展为：

- 双字名称
- 三字名称
- 卦名 + 行为字
- 其他国学、术数、玄学概念

例如：

- 六爻概念可用于状态分析、爻位变化、运行日志
- 梅花概念可用于即时起象、随机性、外应
- 风水概念可用于空间、方位、环境参数
- 奇门概念可用于时间、局、门、星、神、方位

因此，卦LISP 的命名系统不是封闭的六十四卦表，而是一套可以继续生长的玄学语言词库。

## 12. 取象：上下文中的卦义

卦名不应该与某个含义做死板的一对一绑定。

一个卦本身可以有非常多层含义。语言在使用卦名时，不应该把它简化成固定关键字，而应该根据当前模块、作用域、参数形态和运行上下文来解释。

这个机制暂称为：**取象**。

也就是说：

> 卦名 + 当前局 + 上下文 => 当前语义

例如 `益`：

| 上下文 | 取象 |
|---|---|
| 九章 | 加法、累加、增量、数值增加 |
| 周易 | 益卦、风雷益、增益之象 |
| 风水 | 补益、生旺、增强某方位之气 |
| 奇门 | 生助、有利、增势 |
| 运行日志 | 状态增强、势能上升、值变大 |

因此：

```gua
入 九章
益 a b
```

在 `九章` 局中，可以取象为：

```ts
a + b
```

但：

```gua
入 周易
益 问事业
```

在 `周易` 局中，`益` 不再是加法，而可能表示益卦本身、卦辞、爻辞或占断解释。

这意味着卦LISP 的符号系统不是传统编程语言里的固定关键字表，而是一个可随作用域变化的取象系统：

- 同一个卦名可以在不同模块中拥有不同语义
- 同一个卦名可以在不同参数形态下取不同象
- 模块可以注册自己的取象规则
- 运行日志可以记录每一步取了什么象

传统语言通常是：

> 符号绑定到函数。

卦LISP 更接近：

> 符号在局中取象。

## 13. 九章模块

`九章` 是卦LISP 的第一批标准模块之一，定位为计算模块。

它来自中国古代数学传统中的《九章算术》，负责把卦式表达落到确定的数值计算上。

可以暂时这样划分：

- 九章：负责数
- 周易：负责象
- 风水：负责位
- 奇门：负责时、局、门、星、神

`九章` 模块中的取象应该尽量稳定、确定、可测试。

第一版 MVP 可以先让 `九章` 支持基础数值运算：

| 卦名 | 九章取象 | TypeScript |
|---|---|---|
| 益 | 加和 | `+` |
| 损 | 减法 | `-` |
| 丰 | 乘法 | `*` |
| 节 | 除法 | `/` |
| 剥 | 取余 | `%` |
| 升 | 幂 | `**` |

示例：

```gua
入 九章

益 1 2 3
损 10 3
丰 4 5
节 20 4
剥 10 3
升 2 8
```

同时，`九章` 可以借《九章算术》的九个章名提供更高一层的数术函数：

| 章名 | MVP 取象 | 示例 |
|---|---|---|
| 方田 | 平面面积 | `方田 "矩形" 3 4` |
| 粟米 | 比例换算 | `粟米 10 2 5` |
| 衰分 | 按权重分配 | `衰分 120 [萃 1 2 3]` |
| 少广 | 开方、开 n 次方 | `少广 81` / `少广 27 3` |
| 商功 | 体积、工程量 | `商功 "长方体" 2 3 4` |
| 均输 | 均摊、平均 | `均输 10 20 30` |
| 盈不足 | 盈亏术、试差法 | `盈不足 8 2 4 6` |
| 方程 | 二元一次方程组 | `方程 2 1 5 1 -1 1` |
| 勾股 | 直角三角求弦 | `勾股 3 4` |

这些章名不是卦名，而是 `九章` 模块中的数术概念。它们与卦名共同构成 `九章` 之局里的可计算词汇。

例如：

```gua
入 九章

大有 [方田 "矩形" 3 4]
大有 [衰分 120 [萃 1 2 3]]
大有 [方程 2 1 5 1 -1 1]
大有 [勾股 3 4]
```

可编译为 TypeScript 并运行，得到面积、分配结果、方程解和勾股弦长。

### 九章外

`九章外` 是 `九章` 的外库，用来收纳原本不属于《九章算术》体系、但现代计算经常需要的函数。

它类似其他编程语言里的工具库或扩展库，但不用英文 `utils` 命名，而称为：

> 九章外

设计原则：

- `九章` 保留古典数术核心
- `九章外` 补充现代数学与计算函数
- 现代概念不强行附会古籍，能用普通话清楚表达就直接表达
- `入 九章` 后可以继续 `入 九章外`，两个局的取象可以叠加

示例：

```gua
入 九章
入 九章外

大有 [正弦 [节 [圆周率] 2]]
大有 [最大 3 9 2]
大有 [随机]
```

`九章外` 第一版可对齐 TypeScript/JavaScript 的 `Math` 常用能力：

| 名称 | TypeScript | 说明 |
|---|---|---|
| 圆周率 | `Math.PI` | 圆周率 |
| 自然常 | `Math.E` | 自然常数 |
| 根二 | `Math.SQRT2` | 2 的平方根 |
| 半根二 | `Math.SQRT1_2` | 1/2 的平方根 |
| 绝 | `Math.abs` | 绝对值 |
| 上取 | `Math.ceil` | 向上取整 |
| 下取 | `Math.floor` | 向下取整 |
| 四舍 | `Math.round` | 四舍五入 |
| 截整 | `Math.trunc` | 去掉小数部分 |
| 符 | `Math.sign` | 符号 |
| 最大 | `Math.max` | 最大值 |
| 最小 | `Math.min` | 最小值 |
| 随机 | `Math.random` | 随机数 |
| 平方根 | `Math.sqrt` | 平方根 |
| 立方根 | `Math.cbrt` | 立方根 |
| 幂 | `Math.pow` | 幂运算 |
| 指数 | `Math.exp` | 自然指数 |
| 指数减一 | `Math.expm1` | `exp(x) - 1` |
| 对数 | `Math.log` | 自然对数 |
| 常对数 | `Math.log10` | 10 为底对数 |
| 二对数 | `Math.log2` | 2 为底对数 |
| 对数加一 | `Math.log1p` | `log(1 + x)` |
| 正弦 | `Math.sin` | sine |
| 余弦 | `Math.cos` | cosine |
| 正切 | `Math.tan` | tangent |
| 反正弦 | `Math.asin` | arcsine |
| 反余弦 | `Math.acos` | arccosine |
| 反正切 | `Math.atan` | arctangent |
| 方位角 | `Math.atan2` | 二参数反正切 |
| 双曲正弦 | `Math.sinh` | hyperbolic sine |
| 双曲余弦 | `Math.cosh` | hyperbolic cosine |
| 双曲正切 | `Math.tanh` | hyperbolic tangent |
| 反双曲正弦 | `Math.asinh` | inverse hyperbolic sine |
| 反双曲余弦 | `Math.acosh` | inverse hyperbolic cosine |
| 反双曲正切 | `Math.atanh` | inverse hyperbolic tangent |
| 平方和根 | `Math.hypot` | 平方和开根 |
| 整乘 | `Math.imul` | 32 位整数乘法 |
| 前零 | `Math.clz32` | 32 位前导零数量 |
| 单精 | `Math.fround` | 单精度浮点舍入 |
| 半精 | `Math.f16round` | 半精度浮点舍入 |
| 精和 | `Math.sumPrecise` | 精确求和 |

## 14. 连山模块

`连山` 是卦LISP 的数据结构库。

命名取自三易之一的《连山》。这里取“相连、层叠、结构成势”之意，用来表达程序里的数据结构。

可以暂时这样划分：

- 九章：数
- 九章外：现代计算补充
- 连山：结构

`连山` 不强求所有名称都来自卦。数据结构是工程工具，第一原则是清楚、短、可用。因此第一版主要使用简洁中文词：

| 名称 | 取象 | TypeScript/JS 对应 |
|---|---|---|
| 列 | 列表、数组 | `Array` |
| 集 | 集合 | `Set` |
| 典 | 字典、对象 | `Object` |
| 栈 | 栈 | `Array` |
| 队 | 队列 | `Array` |
| 长 | 长度、大小 | `.length` / `.size` |
| 首 | 第一个元素 | `[0]` |
| 尾 | 去首后的剩余 | `.slice(1)` |
| 取 | 按键/下标取值 | `obj[key]` |
| 置 | 返回设置后的新结构 | copy + set |
| 接 | 追加元素 | append |
| 并 | 合并结构 | concat/union/assign |
| 含 | 是否包含 | includes/has |
| 键 | 所有键 | `Object.keys` |
| 值 | 所有值 | `Object.values` |
| 入集 | 集合加入元素 | `Set.add` 的纯函数版 |
| 出集 | 集合移除元素 | `Set.delete` 的纯函数版 |
| 并集 | 集合并集 | union |
| 交集 | 集合交集 | intersection |
| 差集 | 集合差集 | difference |
| 压 | 入栈 | push 的纯函数版 |
| 弹 | 出栈取值 | top |
| 入队 | 入队 | enqueue 的纯函数版 |
| 出队 | 出队取值 | front |

示例：

```gua
入 连山

乾 xs [列 1 2 3]
大有 [长 xs]
大有 [首 xs]
大有 [接 xs 4]

乾 s [集 1 2 2 3]
大有 [含 s 2]
大有 [并集 s [集 3 4]]

乾 user [典 "名" "张三" "岁" 18]
大有 [取 user "名"]
大有 [键 user]
```

`连山` 也可以与 `九章`、`九章外` 叠加：

```gua
入 九章
入 连山

乾 xs [列 1 2 3]
大有 [益 [取 xs 0] [取 xs 1] [取 xs 2]]
```

## 15. 当前标准库格局

当前 MVP 已经形成两个基础方向：

| 领域 | 模块 | 定位 |
|---|---|---|
| 计算 | 九章 | 古典数术核心，负责确定性计算 |
| 计算扩展 | 九章外 | 现代数学与计算补充 |
| 数据结构 | 连山 | 列表、集合、字典、栈、队等结构 |

这意味着卦LISP 已经具备最小程序所需的两个基础能力：

- 能算数
- 能组织数据

从语言设计角度看，现在的基础形态是：

```gua
入 九章
入 九章外
入 连山

乾 xs [列 1 2 3]
大有 [益 [取 xs 0] [取 xs 1] [取 xs 2]]
大有 [正弦 [节 [圆周率] 2]]
```

编译目标：

```ts
const xs = [1, 2, 3]
console.log(xs[0] + xs[1] + xs[2])
console.log(Math.sin(Math.PI / 2))
```

现阶段的语言气质已经比较清楚：

> 用“入”进入某个局，在局中根据上下文取象，用卦名、古籍名、术数名和简洁中文词共同表达程序。

## 16. 接下来还需要补充什么

有了计算、数据结构、函数、条件和显式块之后，卦LISP 已经可以写出最小小程序。

### 1. 控制流

条件分支已经进入 MVP：

```gua
观 [大过 年龄 18] 顶
  大有 "已成年"
观观 [同人 年龄 18]
  大有 "刚成年"
余
  大有 "未成年"
坤
  大有 "观毕"
丁
```

其中：

- `观`：if，观察条件
- `观观`：else if，再观一象
- `余`：else，其余之象
- `坤`：finally/op，落地收束，无论前面分支如何都会执行
- `顶`：开块
- `丁`：收块

还需要补充的是循环：

```gua
循 [小过 i 10]
  大有 i
```

待讨论问题：

- 循环用 `需`、`渐`，还是普通词 `循`？
- 循环是否也使用 `顶/丁`？
- 是否需要 `复` 表示 continue、`夬` 表示 break？

### 2. 函数

函数已经进入 MVP：

```gua
鼎 求和 a b 顶
  归 [益 a b]
丁

大有 [应 求和 3 5]
```

其中：

- `鼎`：函数，像器一样承接输入、在腹中运化、产出结果
- `顶`：取鼎之上，表示函数体开始
- `丁`：与鼎同音，表示函数体结束
- `归`：return，归还结果
- `应`：call，函数应召而动

后续还需要讨论：

- 是否允许默认参数
- 是否允许闭包
- 是否允许高阶函数直接传中文函数名
- `应` 是否长期保留，还是以后允许直接调用

### 3. 作用域与取象规则

现在已经有多模块叠加入局：

```gua
入 九章
入 九章外
入 连山
```

还需要明确：

- 后入之局优先级
- 局部作用域如何创建
- 如何退出一个局
- 不同模块中同名词如何冲突解决
- 是否允许用户自定义取象

### 4. 类型系统

短期可以先不做复杂类型，但至少需要基础类型观念：

- 数
- 文
- 阴阳/真假
- 列
- 集
- 典
- 函数
- 空/未济

如果以后靠 TypeScript 作为主编译目标，可以逐步引入类型标注：

```gua
乾 年龄: 数 18
乾 名: 文 "张三"
```

### 5. 错误处理

需要表达异常、失败、未成、恢复。

可能方向：

```gua
试
  大有 [节 10 0]
救 错
  大有 错
```

也可以从卦象中取名，例如 `坎` 表示陷、险、错误状态，但这部分要谨慎，避免过度玄化导致难用。

### 6. 输入输出

现在 `大有` 暂时表示输出到控制台。

后续还需要：

- 读取文件
- 写入文件
- 网络请求
- 命令行参数
- JSON 读写
- 时间

这部分可能需要一个现代工程库，不宜塞进 `九章` 或 `连山`。

可考虑的库名：

- `行世`：与外部世界交互
- `器用`：工程工具
- `百工`：文件、网络、系统等实用能力

### 7. 玄学核心模块

现在已有计算和数据结构，但项目真正的灵魂还需要玄学模块。

优先级较高的模块可能是：

| 模块 | 定位 |
|---|---|
| 周易 | 起卦、卦辞、爻辞、变卦、互卦 |
| 六爻 | 世应、六亲、六神、用神、动爻 |
| 梅花 | 时间起卦、外应、体用 |
| 风水 | 方位、八宅、九宫、五行生克 |
| 奇门 | 局、门、星、神、盘 |

这些模块不需要一次做完，但需要先选一个作为第一个玄学 MVP。建议从 `周易` 或 `梅花` 开始，因为它们最容易和“卦式表达”直接连接。

### 8. 运行日志与观象

卦LISP 的特色之一应该是可观察运行过程。

未来可以支持：

```bash
gua run examples/jiuzhang.gua --观象
```

输出类似：

```txt
入 九章
益 1 2 3 -> 取象: 加和 -> 6
```

这会让“取象”不只是设定，而是语言运行时可见的一部分。

### 9. 编译器工程化

当前编译器仍是 MVP 级别。

后续需要：

- 正式 tokenizer
- AST
- parser 错误位置
- source map 或源码行号映射
- 测试用例
- 格式化工具
- REPL
- `gua run`
- `gua check`
- `gua fmt`

### 10. 文件组织与包系统

当模块变多后，需要决定：

- `.gua` 文件如何导入其他 `.gua`
- 标准库如何组织
- 用户库如何发布
- `入` 是进入标准库，还是也能引入文件
- `出` 如何导出函数、常量、模块

### 11. 语法块

MVP 已经确定：不使用 Python 式缩进块。

当前采用显式块：

```gua
鼎 名 参数 顶
  ...
丁
```

缩进只作为排版，不参与语义。

`顶/丁` 的来源：

- `鼎` 是函数之器
- 鼎有上、腹、足
- `顶` 与 `鼎` 同音，取其上，表示开器、入腹
- `丁` 与 `鼎` 同音，表示收器、成形

这个设计比普通的 `begin/end` 更贴合卦LISP 的气质。

## 17. 推荐下一阶段目标

函数、条件、块语法已经进入 MVP。下一阶段建议补：

1. **循环**
2. **错误处理**
3. **运行日志与观象**

这三者会让卦LISP 从“能写小程序”走向“能观察程序如何运行”。

推荐的下一个 MVP 示例：

```gua
入 九章
入 连山

坤 i 0

循 [小过 i 5] 顶
  大有 i
  革 i [益 i 1]
丁
```

这段示例需要语言支持：

- 循环
- 变量修改
- 块结构复用
- 运行日志观察每次变化

它会是从“能写小程序”走向“能观察变化”的关键一步。

## 18. MVP v0 语法约定

第一版 MVP 暂时采用非常小的语法集。

### 编译命令

MVP 提供 `gua build` 命令，将 `.gua` 文件编译成 TypeScript：

```bash
./gua.cmd build examples/jiuzhang.gua -o dist/jiuzhang.ts
```

也可以通过 Bun script 调用：

```bash
bun run gua build examples/jiuzhang.gua -o dist/jiuzhang.ts
```

或直接运行命令入口：

```bash
bun bin/gua.mjs build examples/jiuzhang.gua -o dist/jiuzhang.ts
```

如果不指定 `-o`，默认输出到 `dist/<文件名>.ts`。

### Web 编辑器

MVP 提供一个本地 Web 编辑器，技术栈为 Bun + TypeScript。

启动：

```bash
.\web.cmd
```

或：

```bash
bun run web
```

访问：

```txt
http://localhost:4173
```

编辑器设计方向：

- 表格式源码编辑，参考易语言一类表格化编辑体验
- 顶部提供类似 Word Ribbon 的关键词工具栏
- 点击工具栏可以插入 `入`、`鼎`、`观`、`九章`、`连山` 等常用词
- 支持编译为 TypeScript
- 支持直接运行并查看输出

它的目标不是替代专业 IDE，而是帮助新手适应卦LISP 的中文关键字和取象系统。

### 入局

```gua
入 九章
```

表示进入 `九章` 模块之局。后续卦名会在 `九章` 上下文中取象。

`入` 可以叠加多个局：

```gua
入 九章
入 九章外
```

后入之局优先取象；如果后入之局没有该词，就继续向前面的局查找。

### 常量与变量

```gua
乾 a 10
坤 b 5
```

在 MVP 中：

- `乾 名 值` 编译为 TypeScript 的 `const`
- `坤 名 值` 编译为 TypeScript 的 `let`

这里也是取象：`乾` 在声明位置取“确定、不变”之象，在值位置仍可取“真、阳”之象。

### 函数

MVP 使用 `鼎/顶/丁` 定义函数：

```gua
鼎 求和 a b 顶
  归 [益 a b]
丁
```

编译为：

```ts
function 求和(a, b) {
  return a + b
}
```

函数调用使用 `应`：

```gua
大有 [应 求和 3 5]
```

编译为：

```ts
console.log(求和(3, 5))
```

### 条件

MVP 使用 `观/观观/余/坤` 表达条件链：

```gua
观 [大过 年龄 18] 顶
  大有 "已成年"
观观 [同人 年龄 18]
  大有 "刚成年"
余
  大有 "未成年"
坤
  大有 "观毕"
丁
```

编译为：

```ts
if (年龄 > 18) {
  console.log("已成年")
} else if (年龄 === 18) {
  console.log("刚成年")
} else {
  console.log("未成年")
}
console.log("观毕")
```

`坤` 在这里不是 JavaScript 的 `finally` 语句，而是卦LISP 条件结构中的落地收束段：无论前面的观察落在哪一支，最后都会执行。

### 块语法

MVP 不采用 Python 式缩进块。块必须显式开合：

```gua
顶
...
丁
```

目前 `顶/丁` 用于：

- `鼎 ... 顶` 函数体
- `观 ... 顶` 条件体

缩进只用于排版，不参与语义。

### 输出

```gua
大有 [益 a b]
```

`大有` 在 MVP 中暂取“显出、展示”之象，编译为 `console.log(...)`。

### 嵌套表达式

MVP 暂时使用方括号表达嵌套：

```gua
大有 [益 1 [丰 2 3] 4]
```

编译为：

```ts
console.log(1 + (2 * 3) + 4)
```

方括号不是最终语法定案，只是为了让第一版解析器足够简单，方便尽快试出语言味道。

### 数组

MVP 暂时用 `萃` 表示数组聚合：

```gua
萃 1 2 3
```

编译为：

```ts
[1, 2, 3]
```

例如 `衰分` 可以接受权重数组：

```gua
大有 [衰分 120 [萃 1 2 3]]
```
